{
   "$schema": "https://turborepo.org/schema.json",
   "pipeline": {
      "dev": { "outputs": [] },
      "lint": { "outputs": [] },
      "test": { "outputs": [] },
      "build": {
         "dependsOn": ["^build", "lint", "test"],
         "outputs": ["build", "dist"]
      }
   }
}
