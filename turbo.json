{
   "$schema": "https://turborepo.org/schema.json",
   "pipeline": {
      "dev": { "cache": false },
      "lint": { "outputs": [] },
      "test": { "outputs": [] },
      "test:e2e": { "outputs": [] },
      "docs": {
         "dependsOn": ["^lib:build"],
         "outputs": ["swagger.json"]
      },
      "build": {
         "dependsOn": ["^lib:build", "docs", "test"],
         "outputs": ["dist/**"]
      },
      "lib:build": {
         "dependsOn": ["^lib:build"],
         "outputs": ["dist/**"]
      }
   }
}
