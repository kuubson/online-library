{
   "$schema": "https://turborepo.org/schema.json",
   "pipeline": {
      "dev": { "cache": false },
      "lint": { "outputs": [] },
      "build": {
         "dependsOn": ["^build", "docs", "test"],
         "outputs": ["build", "dist"]
      },
      "test": { "dependsOn": ["^build"], "outputs": [] },
      "docs": { "dependsOn": ["^build"], "outputs": ["swagger.json"] }
   }
}
