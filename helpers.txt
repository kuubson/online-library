import casual from 'casual'

for (let x = 0; x < 200; x++) {
    await Book.create({
        title: casual.title,
        author: casual.full_name,
        cover: `https://picsum.photos/${Math.floor(
            Math.random() * (900 - 800 + 1) + 800
        )}/${Math.floor(Math.random() * (700 - 600 + 1) + 600)}`,
        price: x % 2 === 0 ? Math.floor(Math.random() * (2 - 10 + 1) + 10) : null
    })
    console.log(casual.title)
}