import styled from 'styled-components/native'

import { moderateScale } from 'styles'

export const PayPalButton = styled.TouchableOpacity`
   background: #ffc439;
   padding: ${moderateScale(15)}px;
   border-radius: ${moderateScale(15)}px;
`
