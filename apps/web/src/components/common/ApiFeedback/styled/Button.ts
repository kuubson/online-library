import styled from 'styled-components'

import { Submit } from 'styles/styled'

export const Button = styled(Submit).attrs({ 'data-cy': 'apiFeedback-button' })`
   margin-top: 80px;
   padding: 10px 20px;
`
