import styled from 'styled-components'

import utils from 'utils'

export default styled.View`
    width: ${utils.scale(300)}px;
    margin-bottom: ${utils.scale(80)}px;
    justify-content: center;
    align-items: center;
`
