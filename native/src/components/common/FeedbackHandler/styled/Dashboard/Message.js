import styled from 'styled-components'

import Text from 'components/common/Text'

import utils from 'utils'

export default styled(Text)`
    text-align: center;
    line-height: ${utils.scale(30)}px;
`
