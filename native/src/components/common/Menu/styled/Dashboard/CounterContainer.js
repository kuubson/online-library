import styled from 'styled-components'

import utils from 'utils'

export default styled.View`
    width: ${utils.scale(22)}px;
    border-radius: ${utils.scale(22 / 2)}px;
    background: white;
    margin-left: ${utils.scale(10)}px;
    justify-content: center;
    align-items: center;
`
