import styled from 'styled-components'

import utils from 'utils'

export default styled.View`
    height: ${utils.scale(80)}px;
    padding-right: ${utils.scale(23)}px;
    padding-left: ${utils.scale(15)}px;
    background: #0088ff;
    justify-content: space-between;
    align-items: center;
    flex-direction: row;
`
