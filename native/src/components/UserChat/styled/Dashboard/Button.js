import styled from 'styled-components'

import utils from 'utils'

export default styled.TouchableOpacity`
    background: #333333;
    padding: ${utils.scale(5)}px ${utils.scale(15)}px;
    margin-top: ${utils.scale(5)}px;
    border-radius: ${utils.scale(10)}px;
    margin-top: 0px;
`
