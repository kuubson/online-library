import styled from 'styled-components'

import utils from 'utils'

export default styled.TouchableOpacity`
    background: #ffc439;
    padding: ${utils.scale(15)}px;
    border-radius: ${utils.scale(15)}px;
`
