{
   "extends": "../tsconfig/library.json",
   "compilerOptions": {
      "baseUrl": "src",
      "rootDirs": [".", "../../../apps/server"] // NOTE: it should be maybe handled with the "references"
   }
}
